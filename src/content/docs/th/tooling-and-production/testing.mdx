---
title: 'Testing'
description: 'กลยุทธ์ testing แบบเป็นชั้นสำหรับ Svelte — Vitest สำหรับ logic, Testing Library สำหรับ component, ทดสอบ runed module และ Playwright สำหรับ end-to-end'
sidebar:
  order: 4
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## สามชั้น สามเครื่องมือ

การ test แอป Svelte คือ pyramid เดียวกับ frontend ทั่วไป โดยมี helper ที่รู้จัก Svelte ตรงชั้น component:

| ชั้น | เครื่องมือ | ครอบคลุมอะไร |
|-------|------|----------------|
| Unit | Vitest | logic ล้วน, utility และ runed `.svelte.js` module |
| Component | `@testing-library/svelte` + Vitest | component ที่ render ใน jsdom — behaviour ไม่ใช่ internals |
| End-to-end | Playwright | แอปจริงใน browser จริง ข้ามหลายหน้า |

<Mermaid title="testing pyramid สำหรับแอป Svelte" code={pyramidDiagram} />

export const pyramidDiagram = `flowchart TB
  e2e["Playwright: end-to-end (few)"] --> comp["Testing Library: components (some)"]
  comp --> unit["Vitest: logic + runed modules (many)"]`;

## unit test ด้วย Vitest

logic ล้วน ๆ — formatter, การคำนวณ, ฟังก์ชัน update ของ store — ทดสอบด้วย **Vitest** ที่ใช้ config ร่วมกับ Vite จึงเข้าใจโปรเจกต์คุณตั้งแต่แรก:

```js
import { describe, it, expect } from 'vitest';
import { formatPrice } from './money';

describe('formatPrice', () => {
  it('adds a currency symbol', () => {
    expect(formatPrice(5)).toBe('$5.00');
  });
});
```

reactive state ใน module **`.svelte.js`** / `.svelte.ts` (rune นอก component) ทดสอบด้วยวิธีเดียวกัน — Vitest รันด้วย Svelte plugin ให้ rune ทำงานใน test ห่อ assertion ที่ขึ้นกับ effect ด้วย `$effect.root` หรือ flush ด้วย `await tick()` เมื่อจำเป็น

## component test ด้วย Testing Library

**`@testing-library/svelte`** render component เข้า jsdom แล้วให้ query และ interaction ที่สะท้อนพฤติกรรมผู้ใช้ — query ด้วย role/text ไม่ใช่ชื่อ class ภายใน:

```js
import { render, screen } from '@testing-library/svelte';
import userEvent from '@testing-library/user-event';
import { expect, test } from 'vitest';
import Counter from './Counter.svelte';

test('increments on click', async () => {
  render(Counter, { props: { start: 0 } });
  const button = screen.getByRole('button', { name: /count/i });

  await userEvent.click(button);

  expect(button).toHaveTextContent('count: 1');
});
```

วินัยคือ **test behaviour ไม่ใช่ implementation** assert สิ่งที่ผู้ใช้เห็นและทำ (text ที่มองเห็น, role, interaction) เพื่อให้ refactor ที่รักษา behaviour เดิมไว้ไม่ทำ test พัง

## end-to-end ด้วย Playwright

**Playwright** ขับ browser จริงผ่านหน้าจริง — routing, form submission, navigation — ซึ่งเป็นจุดที่ behaviour ฝั่ง server ของ SvelteKit (load function, form action) ต้อง verify:

```js
import { test, expect } from '@playwright/test';

test('user can sign in', async ({ page }) => {
  await page.goto('/login');
  await page.getByLabel('Email').fill('ada@example.com');
  await page.getByRole('button', { name: 'Sign in' }).click();
  await expect(page).toHaveURL('/dashboard');
});
```

เก็บ test พวกนี้ให้น้อยและมีคุณค่าสูง — มันช้ากว่า unit และ component test แต่เป็นชั้นเดียวที่พิสูจน์ว่าทุกอย่างทำงานร่วมกันได้

<Callout title="The one-line takeaway">
test เป็นสามชั้น: **Vitest** สำหรับ logic ล้วนและ runed `.svelte.js` module; **`@testing-library/svelte`** (+ Vitest) สำหรับ component ที่ render ใน jsdom query ด้วย role/text เพื่อ test **behaviour ไม่ใช่ internals**; และ **Playwright** สำหรับ end-to-end flow ที่มีคุณค่าสูงไม่กี่อัน ใน browser จริง (ชั้นที่ verify load function และ form action ของ SvelteKit) unit test เยอะ, component test พอประมาณ, e2e น้อย
</Callout>

export const quizTesting = [
  {
    q: 'หลักการนำทางเวลา test component ด้วย Testing Library คืออะไร?',
    options: [
      'assert state ภายในและตัวแปร private',
      'test behaviour ไม่ใช่ implementation — query ด้วย role/text และ assert สิ่งที่ผู้ใช้เห็น',
      'snapshot HTML ทั้งหมดทุกครั้ง',
      'test แค่ว่ามัน render โดยไม่ crash',
    ],
    answer: 1,
  },
  {
    q: 'จะ test reactive state ใน `.svelte.js` module อย่างไร?',
    options: [
      'test rune ไม่ได้',
      'ด้วย Vitest ที่รัน Svelte plugin ให้ rune ทำงานใน test',
      'ผ่าน browser เท่านั้น',
      'ต้องก๊อป code เข้า component ก่อน',
    ],
    answer: 1,
  },
  {
    q: 'เครื่องมือไหน verify flow sign-in เต็มรูปแบบข้ามหน้าจริงใน browser?',
    options: [
      'Vitest',
      'Playwright (end-to-end)',
      '@testing-library/svelte',
      'svelte-check',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="tooling-and-production/testing" questions={quizTesting} />

<ProgressTracker client:visible id="tooling-and-production/testing" />
